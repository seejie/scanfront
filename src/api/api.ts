const api = '/api/v0/'
export default {
  // 网络概览
  homestatic: `${api}homestatic`,
  // 算力走势
  minerlistbypower: `${api}minerlistbypower`,
  // basefee
  basefee: `${api}basefee`,
  // 搜索信息ID/区块哈希/矿工ID
  queryTarget: `${api}queryTarget`
}
